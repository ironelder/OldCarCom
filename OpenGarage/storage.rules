rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 사진은 누구나 보기(피드 공유), 업로드는 본인 폴더만
    match /users/{uid}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
